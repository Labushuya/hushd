# Detekt V1 — leerer Overlay über default config.
# Mit buildUponDefaultConfig=true wird die eingebaute Default-Config geladen;
# diese Datei kann später Rules anpassen. ignoreFailures=true in build.gradle.kts
# verhindert, dass Findings die Pipeline gaten.
